# config.py

Configuration centralisée pour tous les scrapers.

**Paramètres**: Mode headless, timeouts, délais, proxy, format export, User-Agents.

**Usage**: Modifier `.env` ou importer directement
```python
import config
print(config.HEADLESS_MODE)
```
